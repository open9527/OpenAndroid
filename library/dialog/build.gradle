apply {
    plugin 'com.android.library'
    plugin 'kotlin-android'
    from '../../common.gradle'
}

android {
    defaultConfig {
        // 模块混淆配置
        consumerProguardFiles 'proguard-dialog.pro'
    }
}

