import com.farmer.open9527.buildsrc.Libs

apply {
    plugin 'com.android.library'
    plugin 'kotlin-android'
    plugin 'kotlin-kapt'
    from '../../common.gradle'
}

android {

    defaultConfig {
        // 模块混淆配置
        consumerProguardFiles 'proguard-common.pro'
    }

    sourceSets {
        main {
            // res 资源目录配置
            res.srcDirs(
                    'src/main/res',
            )
        }
    }

}

dependencies {
    api project(':library:base')
    api project(':library:dialog')
    api project(':library:recycleview')
    api project(':library:imageload')
    api project(':library:refresh')
    api project(':widget:common')
    api Libs.Google.Gson
    api Libs.Androidx.Layout.ConstraintLayout
    api Libs.Google.Material
    api Libs.Utils.UtilCode
    api Libs.Androidx.Kotlin.ViewModelKtx
}
